# Reprise de projet — 2026-09-16

> Checkpoint de reprise après pause (~26 jours sans commit).
> Source : lecture du repo au 16/09. Complète `docs/context.md` (source unique).

## État du projet — 2026-09-16

### Tableau de bord
- **Phase** : 4 Développement — Epic 3 backend livré, frontend à démarrer
- **Chantier actif** : aucun (en pause depuis le 21/08)
- **Bloquant** : non
- **Jalon visé** : E4 (Epic 3 complet) → E6 (MVP validé)
- **Avancement MVP** : ~70 % (3 epics backend+UI sur 4 fonctionnels)
- **Tests** : 205 passent, 1 warning PyGI (bénin)

### Ce qui est fait ✅
- **Epic 1 — Auth** : backend + UI login (merge `2a762a0`)
- **Epic 2 — Workouts Cloud → Montre** : backend (`bd68650`) + frontend (`221002b`), validé sur montre réelle. Fix SQLite cross-thread au passage (`8957b98`, ADR-005 révisée).
- **Epic 3 — Activités Montre → Cloud (backend)** : `push_activities`, `list_uploadable_files`, `garmin/client.upload_activity`, `watch/filesystem.absolute_path/file_size`. Contrats figés et validés en réel (smoke FR55 + upload GC). Merge `b87e3d5`.

### Ce qui reste 🔄
- **Epic 3 frontend** : UI zone Montre (liste `list_uploadable_files`, bouton « Synchroniser vers GC » appelant `push_activities` dans un thread worker). Brief de mission à produire.
- **Epic 4 — Historique & logs** : UI transverse (stores existent depuis Epic 1/2).
- **MVP validation (E6)** : workout GC → FR55 + activité FR55 → GC depuis l'UI GTK.
- **Epic 5 — Robustesse** : post-MVP (retry 429, déconnexion USB).

### Points ouverts à trancher avant le frontend Epic 3
1. **UX « première sync »** : l'utilisateur a potentiellement 200+ fichiers sur la montre, dont la plupart déjà sur GC (via Garmin Express). GC fait la déduplication côté serveur, mais l'UX doit guider (sélection, pas d'upload massif aveugle). Décision à prendre avant le brief frontend.
2. **Filtre des `.FIT` acceptés par GC** (US-3.3, Should) : découverte empirique. Démarrer en remontée brute, affiner ensuite.

### Dette technique suivie (post-Epic 2 backend)
- Index unique `(file_name, direction, source)` sur `transferred_files` + `INSERT OR IGNORE`.
- Désinscription callback `WatchDetector.on_status_changed`.
- Garde `WatchFilesystem._resolve` sur chemin relatif.
- Officialisation écart ADR-006 (déclencheur sur label `GARMIN`).
- Pagination `fetch_workouts` si > 20 workouts côté UI.
- Granularité historique `push_workouts` (N entrées « 1/1 » vs agrégée) — à trancher pour Epic 4.

## Plan de reprise

### Étape 0 — Nettoyage (10 min)
- Commiter les fichiers non suivis : `Usage-LLM/Retrospective-OpenRunner55-21-08.md`, `agents/sys-admin.md`.
- Supprimer les branches locales mergées : `feat/epic-2-frontend`, `feat/epic-2-workouts`, `feat/epic-3-activities`, `fix/sqlite-cross-thread`.

### Étape 1 — Cadrage UX première sync (décision)
Trancher le point ouvert n°1 avant de briefé le frontend. Options :
- (a) Upload de tout, GC déduplique côté serveur — simple, mais 200 requêtes inutiles + latence.
- (b) Sélection manuelle par défaut, « tout sélectionner » optionnel — contrôle utilisateur, plus de code UI.
- (c) Comparaison avec `transferred_files` (SQLite) pour ne proposer que les fichiers non déjà remontés — élégant, dépend de la qualité du store.

Recommandation provisoire : **(c)** avec fallback (b) si le store est incomplet. À confirmer avec Franck.

### Étape 2 — Brief frontend Epic 3
Produire `docs/dev/Epic-3-brief-frontend.md` (contrats figés backend, wireframe zone Montre, spec du worker thread). Activer le Dev Frontend.

### Étape 3 — Dev frontend Epic 3
Ouvrir `feat/epic-3-frontend`, implémenter, gate (tests + revue + validation runtime sur montre réelle), merge.

### Étape 4 — Epic 4 (Historique & logs)
UI transverse, backend quasi-néant.

### Étape 5 — MVP validation (E6)
Condition : workout GC → FR55 + activité FR55 → GC depuis l'UI GTK, sur matériel réel.

## Notes méthode (tirées de la rétrospective 21/08)
- Pratiques à haut levier à conserver : spec avant code, gates avec validation runtime, ADR vivants, un chantier à la fois.
- À abandonner : grilling 50-60 tours, scripts quality-check, méta-boucle, personas au-delà de 4.
- À ajouter : rétro courte après chaque gate d'epic (3 questions), tableau de suivi du temps par epic.

---

*Directeur de Projet — Checkpoint de reprise. Document ponctuel, à compléter par `docs/context.md` (source unique mise à jour à chaque jalon).*
