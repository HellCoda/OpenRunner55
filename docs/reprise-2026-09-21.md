# Reprise de projet — 2026-09-21

> Checkpoint de reprise après merge Epic 5.
> Source : lecture du repo au 21/09. Complète `docs/context.md` (source unique).

## État du projet — 2026-09-21

### Tableau de bord
- **Phase** : 4 Développement — Epic 5 livré et mergé, MVP+ fonctionnel
- **Chantier actif** : aucun
- **Bloquant** : non
- **Avancement** : 5 epics sur 5 livrés (Epics 1-5 mergés sur `main`)
- **Tests** : 298 verts (`pytest tests/ -m unit`)

### Ce qui est fait ✅
- **Epic 1 — Auth** : backend + UI login (merge `2a762a0`)
- **Epic 2 — Workouts Cloud → Montre** : backend + frontend, validé sur montre réelle
- **Epic 3 — Activités Montre → Cloud** : backend + frontend, validé sur montre réelle
- **Epic 4 — Historique & logs** : UI transverse (historique + logs filtrables) (merge `cab70b9`)
- **Epic 5 — Sync auto & UX quotidienne** : 4 chantiers (merge `ea248bc`)
  - 409 Duplicate → skip (fix backend)
  - Refresh listes (boutons ↻ + refresh auto après sync)
  - Masquage fichiers déjà transférés par défaut (toggle + compteur)
  - Sync auto au branchement de la montre
- **Fixs UX post-tests réels** : taille fenêtre 1200×750, timezone logs, espacement bandeau montre

### Ce qui reste en maintenance / reporté ⏳

| Item | Nature | Priorité |
|------|--------|----------|
| Dette : exception domaine pour le 409 (`garminconnect` importé dans `sync/`) | Technique | Basse |
| Retry 5xx (ADR-007 couvre 429 seulement) | Robustesse | Moyenne |
| Granularité historique (N entrées « 1/1 » vs agrégée) | UX | Basse |
| Uniformisation globale timestamps (UTC vs localtime) | Technique | Basse |
| Cosmétique : curseurs `Gtk.Paned`, placement bandeau | UX | Basse |
| Sync auto descendante (workouts GC → montre) | Feature | Si besoin |
| Vue « Compte & Paramètres » (US-1.3) | Feature | Si besoin |
| Packaging AppImage (ADR-009) | DevOps | Pour publication |
| Fichiers non suivis : `tests/test-21-09.txt`, `tests/test-epic3.txt` | Git | À committer ou gitignore |

### Points ouverts à trancher avant la prochaine session
1. **Validation réelle Epic 5** : la sync auto au branchement a été testée en réel par Franck (OK). Les autres chantiers (409, refresh, grisement) également validés visuellement.
2. **Direction du projet** : le MVP+ est fonctionnel et validé. Trois pistes possibles :
   - (a) **Robustesse** : traiter le retry 5xx, la déconnexion USB en cours de transfert, l'intégrité des transferts (Epic 5 initial « Robustesse » non couvert).
   - (b) **Publication** : packaging AppImage (ADR-009), README, licence, premier release.
   - (c) **Features** : vue Compte & Paramètres, sync auto descendante, export logs.

## Plan de reprise

### Étape 0 — Nettoyage (5 min)
- Commiter ou gitignoreer `tests/test-21-09.txt` et `tests/test-epic3.txt`.
- Supprimer la branche locale `feat/epic-5-sync-auto` (mergée).

### Étape 1 — Choisir la direction
Décider entre robustesse, publication ou features. Recommandation DP : **(b) publication** — le MVP+ est fonctionnel et validé en réel. Un AppImage + README propre permet de partager l'app et de boucler la boucle. La robustesse peut venir ensuite.

### Étape 2 — Selon la direction choisie
- **Publication** : activer l'agent DevOps/sys-admin pour l'AppImage, rédiger le README, choisir une licence.
- **Robustesse** : créer un Epic 6 « Robustesse » (retry 5xx, déconnexion USB, intégrité).
- **Features** : spécifier la vue Compte & Paramètres ou la sync auto descendante.

## Références clés dans le repo

- `docs/context.md` — contexte global (source unique)
- `docs/dev/Epic-5-brief.md` — brief Epic 5
- `docs/dev/Epic-5-notes.md` — notes de dev (choix, écarts, points à trancher)
- `docs/dev/backlog-post-epic-4.md` — backlog (items traités/maintenance)
- `docs/cadrage/epics.md` — découpage en epics (Epic 5 redéfini)
- `docs/decisions/` — ADR 001 à 009

---

*Directeur de Projet — Checkpoint de reprise. Document ponctuel, à compléter par `docs/context.md` (source unique mise à jour à chaque jalon).*
